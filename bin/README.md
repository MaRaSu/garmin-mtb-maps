# Binary Directory

This directory contains binary tools needed for map generation.

## gmt

Place the Ubuntu CLI version of GMapTool here as `gmt`.

This tool is required for merging multi-layer Garmin maps (routing, bottom, and main layers).

The binary should be executable and compatible with linux/amd64 platform.

**Location:** `bin/gmt`
